# SwitchPay Leadcatcher

Sistema de captação rápida para eventos.

## Como rodar o painel automático
1. Execute `yarn start` em um terminal.
2. Execute `node run-tunnel.js` em outro terminal.
3. Abra `localhost:3000/display` no navegador.
